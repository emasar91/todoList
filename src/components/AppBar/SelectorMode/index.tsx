import { Box } from '@mui/material'
import SwitchMode from '../SwitchMode'

const SelectorMode: React.FC<{}> = () => {
  return (
    <Box>
      <SwitchMode />
    </Box>
  )
}

export default SelectorMode
